{
  "version": 2,
  "installCommand": "corepack enable && corepack yarn install --immutable && corepack yarn workspace @scholar-flow/backend db:generate",
  "buildCommand": "corepack yarn workspace @scholar-flow/backend build",
  "outputDirectory": "apps/backend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/apps/backend/api/index.js"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
