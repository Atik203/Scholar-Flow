{
  "version": 2,
  "build": {
    "env": {
      "ENABLE_EXPERIMENTAL_COREPACK": "1"
    }
  },
  "installCommand": "corepack enable && corepack yarn --version && corepack yarn install --immutable",
  "buildCommand": "corepack enable && corepack yarn workspace @scholar-flow/backend db:generate && corepack yarn workspace @scholar-flow/backend build",
  "outputDirectory": "apps/backend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/apps/backend/api/index.js"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
